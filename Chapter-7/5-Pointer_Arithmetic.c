#include<stdio.h>

 int main() {
     /*int i =444;
     int *ptr=&i;
     printf("The value of ptr is %u \n:",ptr);
    //  ptr--; //ptr = ptr-1
    //  ptr--;
     ptr++; //ptr = ptr+1
    //  ptr++;
    //  ptr++;
     printf("The value of ptr is %u \n:",ptr);


     char c ='44';
     char *ptr=&c;
     printf("The value of ptr is %u \n:",ptr);
    //  ptr--; //ptr = ptr-1
    //  ptr--;
     ptr++; //ptr = ptr+1
    //  ptr++;
    //  ptr++;
     printf("The value of ptr is %u \n:",ptr);
     */
     float c =4.4;
     float *ptr=&c;
     printf("The value of ptr is %u \n:",ptr);
    //  ptr--; //ptr = ptr-1
    //  ptr--;
     ptr++; //ptr = ptr+1
    //  ptr++;
    //  ptr++;
     printf("The value of ptr is %u \n:",ptr);
    return 0;
}